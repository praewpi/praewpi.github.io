# praewpi.github.io
Personal portfolio website built using HTML, CSS, and JavaScript.

The project currently uses a JSON file as a data source, which is dynamically rendered with JavaScript to display content across different sections. It also integrates Supabase as a backend service to manage and update a like counter.description
 
The website showcases my skills, experience, and hobbies.